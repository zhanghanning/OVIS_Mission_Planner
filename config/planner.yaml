server:
  host: 0.0.0.0
  port: 8081
  public_base_url: http://127.0.0.1:8081

storage:
  base_dir: .
  job_dir: data/jobs
  package_dir: data/packages
  result_dir: data/results

planner:
  planner_type: multi_robot_global
  poll_timeout_sec: 120
  graph_weight_key: weight

ros_bridge:
  result_dir: /planner_results
  map_frame: map
