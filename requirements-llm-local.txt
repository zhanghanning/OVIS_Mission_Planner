# torch is installed explicitly in deployment instructions so the wheel channel matches the target runtime.
transformers>=4.52,<5.0
accelerate>=1.0,<2.0
sentencepiece>=0.2,<1.0
Pillow>=10.0,<12.0
bitsandbytes>=0.45,<1.0
peft>=0.14,<1.0
datasets>=3.0,<4.0
tensorboard>=2.17,<3.0
